apiVersion: apiserver.config.miloapis.com/v1alpha1
kind: BillingOperator
metricsServer:
  bindAddress: "0"

webhookServer:
  tls:
    secretRef:
      name: billing-webhook-cert
      namespace: billing-system

natsConfig:
  url: nats://nats.nats-system.svc:4222
