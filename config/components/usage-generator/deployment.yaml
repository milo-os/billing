# SPDX-License-Identifier: AGPL-3.0-only
# Deploys the usage-generator as a standalone pod.
# Staging only — never apply in production.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: billing-usage-generator
  labels:
    app.kubernetes.io/name: billing
    app.kubernetes.io/component: usage-generator
    app.kubernetes.io/managed-by: kustomize
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: billing
      app.kubernetes.io/component: usage-generator
  template:
    metadata:
      labels:
        app.kubernetes.io/name: billing
        app.kubernetes.io/component: usage-generator
        billing.miloapis.com/emits-usage: "true"
    spec:
      serviceAccountName: billing-controller-manager
      securityContext:
        runAsNonRoot: true
      containers:
        - name: usage-generator
          image: ghcr.io/milo-os/billing:dev
          command:
            - /billing
          args:
            - usage-generator
            - --server-config=/config/config.yaml
            - --endpoint=$(ENDPOINT)
            - --interval=$(INTERVAL)
            - --bindings=$(BINDINGS)
            - --meters=$(METERS)
            - --health-probe-bind-address=:8081
          env:
            - name: ENDPOINT
              value: ""
            - name: INTERVAL
              value: "30s"
            - name: BINDINGS
              value: ""
            - name: METERS
              value: ""
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8081
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8081
            initialDelaySeconds: 5
            periodSeconds: 10
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - "ALL"
          resources:
            limits:
              cpu: 100m
              memory: 64Mi
            requests:
              cpu: 10m
              memory: 32Mi
          volumeMounts:
            - name: config
              mountPath: /config
            - name: trust-bundle
              mountPath: /etc/kubernetes/pki/trust
              readOnly: true
            - name: discovery-kubeconfig
              mountPath: /etc/kubernetes/project-discovery/auth
              readOnly: true
            - name: client-cert
              mountPath: /etc/kubernetes/projects/pki/client
              readOnly: true
      terminationGracePeriodSeconds: 30
      volumes:
        - name: config
          configMap:
            name: billing-config
        - name: trust-bundle
          configMap:
            name: datum-control-plane-trust-bundle
        - name: discovery-kubeconfig
          configMap:
            name: billing-discovery-kubeconfig
        - name: client-cert
          csi:
            driver: csi.cert-manager.io
            readOnly: true
            volumeAttributes:
              csi.cert-manager.io/common-name: system:control@billing.miloapis.com
              csi.cert-manager.io/fs-group: "65532"
              csi.cert-manager.io/issuer-kind: ClusterIssuer
              csi.cert-manager.io/issuer-name: datum-control-plane
              csi.cert-manager.io/key-usages: client auth
              csi.cert-manager.io/organizations: system:masters
