# Resources installed into the Milo control plane (milo-system namespace)
# alongside billing's CRDs. The companion FluxCD Kustomization in
# datum/infra targets this directory via its own OCI source.
resources:
  - service.yaml
  - service-configuration.yaml
