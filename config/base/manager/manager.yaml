apiVersion: apps/v1
kind: Deployment
metadata:
  name: billing-controller-manager
  labels:
    control-plane: billing
    app.kubernetes.io/name: billing
    app.kubernetes.io/managed-by: kustomize
spec:
  selector:
    matchLabels:
      control-plane: billing
  replicas: 2
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/default-container: manager
      labels:
        control-plane: billing
        app.kubernetes.io/name: billing
    spec:
      securityContext:
        runAsNonRoot: true
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                topologyKey: kubernetes.io/hostname
                labelSelector:
                  matchLabels:
                    control-plane: billing
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              control-plane: billing
      containers:
      - command:
        - /billing
        args:
          - operator
          - --leader-elect
          - --leader-elect-namespace=kube-system
          - --health-probe-bind-address=:8081
          - --server-config=/config/config.yaml
        image: ghcr.io/milo-os/billing:dev
        name: manager
        ports:
        - containerPort: 9443
          name: webhook-server
          protocol: TCP
        - containerPort: 8443
          name: metrics
          protocol: TCP
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - "ALL"
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8081
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8081
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          limits:
            cpu: 500m
            memory: 128Mi
          requests:
            cpu: 10m
            memory: 64Mi
        volumeMounts:
        - name: config
          mountPath: /config
        - name: webhook-cert
          mountPath: /tmp/k8s-webhook-server/serving-certs
          readOnly: true
        - name: nats-certs
          mountPath: /etc/nats-certs
          readOnly: true
      serviceAccountName: billing-controller-manager
      terminationGracePeriodSeconds: 10
      volumes:
      - name: config
        configMap:
          name: billing-config
      # Optional so the manager can run without admission webhooks: when
      # `webhookServer:` is omitted from the server config, the binary
      # skips the webhook server entirely and the missing Secret is fine.
      - name: webhook-cert
        secret:
          secretName: billing-webhook-cert
          defaultMode: 420
          optional: true
      - name: nats-certs
        csi:
          driver: csi.cert-manager.io
          readOnly: true
          volumeAttributes:
            csi.cert-manager.io/issuer-name: nats
            csi.cert-manager.io/issuer-kind: ClusterIssuer
            csi.cert-manager.io/common-name: billing-operator.nats.client
            csi.cert-manager.io/subject-organizations: datum
            csi.cert-manager.io/fs-group: "65532"
