apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: vector-agent
  namespace: vector-agent
  labels:
    app.kubernetes.io/name: vector-agent
    app.kubernetes.io/managed-by: kustomize
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: vector-agent
  template:
    metadata:
      labels:
        app.kubernetes.io/name: vector-agent
    spec:
      serviceAccountName: vector-agent
      # Tolerate all taints so every node contributing usage events has an agent.
      # SRE may restrict to specific taints for environments with sensitive workloads.
      tolerations:
        - operator: Exists
      volumes:
        - name: config
          configMap:
            name: vector-agent-config
        - name: data
          hostPath:
            path: /var/lib/vector-agent
            type: DirectoryOrCreate
        - name: token
          projected:
            sources:
              - serviceAccountToken:
                  audience: billing-gateway
                  expirationSeconds: 3600
                  path: token
      initContainers:
        - name: init-data
          image: busybox:1.36
          command: ["sh", "-c", "chown -R 1000:1000 /var/lib/vector"]
          volumeMounts:
            - name: data
              mountPath: /var/lib/vector
      containers:
        - name: vector
          image: timberio/vector:0.45.0-alpine
          command: ["/bin/sh", "-c"]
          args:
            - |
              export VECTOR_AUTH_TOKEN=$(cat /var/run/secrets/billing-gateway/token)
              exec /usr/local/bin/vector --config /etc/vector/vector.toml
          env:
            - name: BILLING_GATEWAY_URL
              value: "https://billing-gateway.billing-system.svc.cluster.local:8080"
          ports:
            - name: cloudevents
              containerPort: 9880
              protocol: TCP
          volumeMounts:
            - name: config
              mountPath: /etc/vector
            - name: data
              mountPath: /var/lib/vector
            - name: token
              mountPath: /var/run/secrets/billing-gateway
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 256Mi
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
            runAsNonRoot: true
            allowPrivilegeEscalation: false
