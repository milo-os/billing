apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
namespace: milo-system
configMapGenerator:
  - name: billing-resource-metrics
    files:
      - billing_account_bindings.yaml
    options:
      labels:
        telemetry.datumapis.com/core-resource-metrics-config: "true"
